# ================== API ==================
# Base URL của ladipage-backend (bao gồm prefix /api)
NEXT_PUBLIC_API_URL=http://localhost:7002/api

# Bật MSW mock API để FE chạy độc lập khi BE chưa sẵn sàng
NEXT_PUBLIC_API_MOCKING=false

# AI-SEO: true = gọi Nest /api/ai-seo/* ; false = Next BFF /api/ai-seo/*
NEXT_PUBLIC_AI_SEO_USE_NEST=false
# Chat/conversations vẫn dùng BFF cho đến khi BE có module
NEXT_PUBLIC_AI_SEO_CHAT_USE_BFF=true

# Stripe publishable key (cho Embedded Checkout modal)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_key

# ================== SUPABASE ==================
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ-your-anon-key
# Server-side only — required for landing page CRUD, builder publish, templates admin
# Supabase Dashboard → Settings → API → service_role (never expose to client)
SUPABASE_SECRET_KEY=eyJ-your-service-role-key
# Alias accepted by some routes/scripts:
# SUPABASE_SERVICE_ROLE_KEY=eyJ-your-service-role-key
# Direct Postgres URL for running landing migrations (pnpm db:landing-migrate)
# DATABASE_URL=postgresql://postgres.[ref]:[pass]@aws-0-[region].pooler.supabase.com:5432/postgres

# ================== AUTH ==================
# legacy = POST /auth/login + captcha (cần USE_SUPABASE_AUTH=false trên BE)
# supabase = Supabase signIn + POST /auth/exchange
NEXT_PUBLIC_AUTH_MODE=legacy

NEXT_PUBLIC_SESSION_COOKIE_NAME=ladipage-session
NEXT_PUBLIC_FB_SESSION_COOKIE_NAME=ladipage-fb-session
NEXT_PUBLIC_SB_REFRESH_COOKIE_NAME=ladipage-sb-refresh

# Bật mock Facebook auth khi dev không có extension
NEXT_PUBLIC_AUTH_MOCK=false

# ================== FACEBOOK (cho /facebook-ads) ==================
NEXT_PUBLIC_FB_GRAPH_VERSION=v21.0
# ================== LANDING PUBLISH / DOMAIN (Plan A free subdomain) ==================
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Keep false until DNS wildcard + Worker are ready; local uses /p/{slug} only
LANDING_FREE_SUBDOMAIN_ENABLED=false
FREE_SITE_DOMAIN=
NEXT_PUBLIC_FREE_SITE_DOMAIN=
# FREE_SUBDOMAIN_DELIVERY=proxy
# FREE_SUBDOMAIN_DELIVERY=r2
LANDING_CUSTOM_DOMAIN_EDGE_ENABLED=false
# LANDING_REVALIDATE_SECRET=
# LANDING_ORIGIN_URL=http://localhost:3000

# domain + landing publish (Plan A free subdomain): prod
# LANDING_FREE_SUBDOMAIN_ENABLED=true
# FREE_SITE_DOMAIN=liora.app
# NEXT_PUBLIC_FREE_SITE_DOMAIN=liora.app
# FREE_SUBDOMAIN_DELIVERY=proxy
# NEXT_PUBLIC_APP_URL=https://app.liora.app
# Plan B — customer domain edge
# LANDING_CUSTOM_DOMAIN_EDGE_ENABLED=true
# CUSTOM_DOMAIN_CNAME_TARGET=fallback.liora.app
# CLOUDFLARE_SAAS_FALLBACK_ORIGIN=fallback.liora.app
# CLOUDFLARE_ACCOUNT_ID=
# CLOUDFLARE_ZONE_ID=
# CLOUDFLARE_API_TOKEN=
# CLOUDFLARE_LANDING_ROUTES_KV_ID=
# CLOUDFLARE_LANDING_EDGE_WORKER=liora-landing-edge
