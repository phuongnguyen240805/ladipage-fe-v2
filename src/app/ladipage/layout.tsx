import type { ReactNode } from "react";

/** Minimal chrome — editor tab only, no admin sidebar. */
export default function LadipageEditorLayout({ children }: { children: ReactNode }) {
  return children;
}
